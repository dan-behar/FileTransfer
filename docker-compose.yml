version: '3.9'

services:
  mongo:
    image: mongo:latest
    ports:
      - 27017:27017
    restart: always
    volumes:
      - mongo_vols:/data/db
  
  # api:
  #   build: .
  #   ports:
  #     - 80:8080
  #   depends_on:
  #     - mongo

volumes:
  mongo_vols: {}